# 7.3 — Unit tests for pure helpers

> **Goal:** input → output tests for the pure helpers. No dependencies — straightforward, fast, target 100%.
>
> **Depends on:** nothing. **Feeds:** nothing directly (these are leaves).

## What to write

Pure helpers to cover (all under `apps/mailops/src/`):

| File | Functions | Notes |
|---|---|---|
| `lib/tracking/pixel.ts` | `generateTrackingPixel` | one input → one URL/buffer shape |
| `lib/tracking/link-wrap.ts` | `wrapLinksWithTracking`, `addTrackingToEmail` | inject `createLink` callback; covers `<a href>` rewriting + pixel append |
| `lib/tracking/stats.ts` | `calculateRates` | **port the assertions from Phase 0's rate-math canary** (`tracking-service.test.ts` case 7) so the math stays pinned |
| `lib/email-subject.ts` | `determineEmailSubject` | pure except thread-subject fallback (mock the repo) |
| `lib/placeholders/index.ts` | `replacePlaceholders`, `validatePlaceholders`, `extractPlaceholders` | pure; Phase 0 Group L already pins 10 cases — port them |
| `services/inbox-sync/classify.ts` | predicates + `determineNotificationType` + history-gap math | inject `hasOriginalForThread` |
| `services/inbox-sync/states.ts` | `nextContactStatus` | REPLY→REPLIED, BOUNCE→BOUNCED, else null |
| `services/inbox-sync/apply-classification.ts` | `applyClassification` | takes `{ change, deps }`; inject event/contact/thread repos |

### Porting from Phase 0

Phase 0's Groups L (placeholders) + M (email-subject) + parts of B (rate math) are already pure-function characterization tests. **Port their assertions** into the new `unit/lib/` files (same cases, cleaner home). The characterization files get deleted in 7.9 — this is where their coverage lives permanently.

## Files

**Create:** `apps/mailops/src/__tests__/unit/lib/{pixel,link-wrap,stats,email-subject,placeholders,schedule}.test.ts` + `apps/mailops/src/__tests__/unit/inbox-sync/{classify,states,apply-classification}.test.ts`.

## Definition of done

- [ ] Every pure helper file has a test; 100% line coverage on `pixel`, `link-wrap`, `stats`, `placeholders`, `email-subject`.
- [ ] Rate math canary ported (calculateRates pinned to the same values Phase 0 asserted).
- [ ] `npm test -w mailops` green; `tsc --noEmit` clean; lint clean.

## Commit

```
phase 7.3: unit tests for pure helpers
```
